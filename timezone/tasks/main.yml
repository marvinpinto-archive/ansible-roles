---
- name: 'Set the time zone'
  sudo: yes
  copy:
    content: |
      {{ timezone_string }}
      
    dest: '/etc/timezone'
    owner: 'root'
    group: 'root'
    mode: '0644'
  notify: 'Update the local timezone'
